// Both the API and fallback sources need at least this many valid questions.
export const MIN_QUESTIONS = 10;
export const QUIZ_DURATION_SECONDS = 10 * 60;
export const MIN_LOADING_DISPLAY_MS = 1000;

// The current product uses 10 questions in each quiz session.
export const QUESTIONS_PER_QUIZ = MIN_QUESTIONS;
